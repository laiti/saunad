import Telegram from '../../../source/lib/api';
import  Telegram from '../../../source/lib/api';

describe('Telegram', () => {
  test('Should send message', () => {
    const Telegram = new Telegram(config);
  });
});